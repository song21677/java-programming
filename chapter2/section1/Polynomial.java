// 다항함수(polynomial)는 항(term)들의 합이다.

public class Polynomial {
    // 다항식의 이름
    public char name;
    // 다항식을 구성하는 항들을 저장할 배열
    public Term [] terms;
    // 다항식을 구성하는 항의 개수
    public int nTerms = 0;
}
